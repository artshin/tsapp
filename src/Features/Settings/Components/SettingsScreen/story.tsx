import React from 'react'
import { storiesOf } from '@storybook/react-native'
import { SettingsScreen } from './index'

storiesOf('Screens', module).add('Settings', () => {
  return <SettingsScreen />
})
